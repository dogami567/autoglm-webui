# Copy to docker.env and fill in your own values (docker.env is git-ignored).
#
# Model service options:
PHONE_AGENT_BASE_URL=https://open.bigmodel.cn/api/paas/v4
PHONE_AGENT_MODEL=autoglm-phone
PHONE_AGENT_API_KEY=your-bigmodel-api-key
#
# If you run the local vLLM container on the same host (docker_model_vllm_up.bat):
# PHONE_AGENT_BASE_URL=http://host.docker.internal:8000/v1
# PHONE_AGENT_MODEL=autoglm-phone-9b
# PHONE_AGENT_API_KEY=EMPTY
#
# Device options:
# PHONE_AGENT_DEVICE_TYPE=adb
# PHONE_AGENT_DEVICE_ID=192.168.1.100:5555
# PHONE_AGENT_LANG=cn
# PHONE_AGENT_MAX_STEPS=100
